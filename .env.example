# Copy to .env for local development. Django loads .env; environment overrides it.
# Generate a secret: python -c "import secrets; print(secrets.token_urlsafe(64))"
DJANGO_DEBUG=true
DJANGO_SECRET_KEY=replace-with-a-generated-secret
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
